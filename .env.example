# Backend (.env in Backend/ folder)
# Copy to Backend/.env and set values
PORT=5000

# Optional (backend works without these for prototype):
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# -----
# Frontend (for local non-Docker run)
# Copy to Frontend/.env if running npm start outside Docker
# REACT_APP_API_URL=http://localhost:5000
